package Strings;
import java.util.*;
public class Equals // Class for Equal for == of two strings
{
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in); // Scanner class
        String str=new String(); // Taking input from console
        String str1=new String();
        System.out.println(str==str1); // Eqalling the two strings
        sc.close(); // scanner class close.
    }
}
